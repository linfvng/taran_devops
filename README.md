# Take-home

1. Read [`TASK.md`](TASK.md).
2. The service you are deploying is in [`app/`](app/) — see [`app/README.md`](app/README.md) for its
   endpoints and environment variables.
3. Check your tooling: `docker`, `minikube`, `kubectl`, `helm`, `terraform`.
