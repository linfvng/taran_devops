Score API "{{ .Release.Name }}" deployed.

{{- if .Values.ingress.enabled }}

Reachable via Ingress at:
  http://{{ .Values.ingress.host }}{{ .Values.ingress.path }}

If you're on minikube, make sure:
  1. The ingress addon is enabled: minikube addons enable ingress
  2. {{ .Values.ingress.host }} resolves to `minikube ip` (edit /etc/hosts, or use
     `curl --resolve {{ .Values.ingress.host }}:80:$(minikube ip)`).

{{- else }}

Ingress is disabled. Reach the service with:
  kubectl port-forward svc/{{ include "score-api.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}

Check pod health:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}